export enum OperationType {
  UPLOAD = 'UPLOAD',
  DOWNLOAD = 'DOWNLOAD',
  DELETE = 'DELETE',
  RENAME = 'RENAME',
  UPDATE_DESC = 'UPDATE_DESC',
  LOGIN = 'LOGIN',
  UPDATE_USER = 'UPDATE_USER',
}

export enum TargetType {
  FILE = 'FILE',
  USER = 'USER',
}
